openpyxl
reportlab

# openpyxl serialises XML with lxml when it is installed and with xml.etree when
# it is not, and the two differ on whitespace in self-closing tags (<tag/> against
# <tag />). Left undeclared, the workbook fails to reproduce anywhere lxml is absent.
lxml>=5.0
